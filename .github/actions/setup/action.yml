name: Setup
description: Setup project

runs:
  using: composite
  steps:
    - name: Setup pnpm
      uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061 # v4.2.0

    - name: Setup Node.js
      uses: actions/setup-node@2028fbc5c25fe9cf00d9f06a71cc4710d4507903 # v6.0.0
      with:
        node-version-file: .node-version
        cache: pnpm

    - name: Install dependencies
      shell: bash
      run: pnpm install --frozen-lockfile

    - name: Copy .env
      shell: bash
      run: cp apps/web/.env apps/web/.env.local

    - name: Copy wrangler.json
      shell: bash
      run: |
        cp apps/overlay/wrangler.example.json apps/overlay/wrangler.json
        cp apps/web/wrangler.example.json apps/web/wrangler.json
        cp apps/worker-cdn/wrangler.example.json apps/worker-cdn/wrangler.json
