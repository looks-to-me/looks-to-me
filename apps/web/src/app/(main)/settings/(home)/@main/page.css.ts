import { style } from '@vanilla-extract/css';

export const wrapper = style({
  padding: '16px',
  maxWidth: '600px',
  margin: '0 auto',
});

export const title = style({
  fontWeight: '400',
  fontSize:' 1.5rem',
  margin: '0 0 10px 0',
});
