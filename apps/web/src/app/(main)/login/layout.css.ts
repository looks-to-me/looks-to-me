import { style } from '@vanilla-extract/css';

export const main = style({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  width: '100%',
  height: '100%',
  padding: '16px',
});
