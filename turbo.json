{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [
    "**/.env",
    "**/.env.*local"
  ],
  "tasks": {
    "generate": {
      "outputs": [
        "src/graphql/generated/**",
        "worker-configuration.d.ts"
      ]
    },
    "build": {
      "dependsOn": [
        "^build",
        "generate"
      ],
      "outputs": [
        "dist/**"
      ]
    },
    "build:app": {
      "dependsOn": [
        "^build",
        "generate"
      ],
      "outputs": [
        ".next/**",
        "dist/**"
      ]
    },
    "test": {
      "dependsOn": [
        "^build",
        "generate"
      ]
    },
    "lint:code": {
      "dependsOn": [
        "^build",
        "generate"
      ]
    },
    "lint:type": {
      "dependsOn": [
        "^build",
        "generate"
      ]
    }
  }
}
