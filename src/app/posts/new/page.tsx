import { UploadImage } from '../../_components/upload-image';

import type { FC } from 'react';

const PostsNew: FC = () => {
  return (
    <main>
      <UploadImage />
    </main>
  );
};

export default PostsNew;
