import { Header } from './_components/header';

import type { FC } from 'react';

const HomePage: FC = () => {
  return (
    <div>
      <Header>
        Header
      </Header>
      <main>
        MainContent
      </main>
    </div>
  );
};

export default HomePage;
